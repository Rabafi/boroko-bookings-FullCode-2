# Deployment Evidence Matrix

As of: 2026-07-13

This file records release and deployment evidence for the current Tsa Bonno HospitalityOS worktree. It is not a substitute for the release runbook; it is the place where the runbook's deployment matrix is made explicit.

Status values:

- `proved`: current evidence proves the row for this worktree/date.
- `local-only`: local code/build/test evidence exists, but publication or production smoke is not proven.
- `not-proven`: no current evidence in this worktree proves the row.

| Surface | Local verification | Published or deployed | Smoke-tested | Current evidence | Remaining proof required |
| --- | --- | --- | --- | --- | --- |
| Supabase migrations and functions | proved | proved for linked project | proved for dashboard identity, brand-label RPC, and function deployment | Migration `20260713013000` appears in both local and remote history. Live anonymous RPC calls return Tsa Bonno LodgingOS, HotelOS, and Restaurant & Bar POS. `send-booking-confirmation` is redeployed with Tsa Bonno copy. Authenticated Management API and CLI verification report project display name `Tsa Bonno HospitalityOS` while ref `oicgpknsmtvcsjacymum` remains unchanged. | Run isolated customer-data/provider smoke where required. |
| Desktop product family | proved | prior releases only; renamed release not published | local packaged asar isolation + metadata proved; disposable user-data load proved; operator/clean-machine/NSIS in-place upgrade smoke not-proven | LodgingOS, HotelOS, and Restaurant & Bar POS compile under isolated `out/<product>` trees and package only their own output. Independent `@electron/asar` inspection of each `dist/*/win-unpacked/resources/app.asar` found zero foreign `out/<other-product>/` paths, correct packaged mains, correct `product.json`, correct preserved update repos in `app-update.yml`, official product logo resources, bridge package names (`boroko-bookings` etc.), and `deleteAppDataOnUninstall: false`. Fresh v1.5.5 installers have branded icons and exact Tsa Bonno `ProductName`/`FileDescription` metadata. Automated packaging-isolation coverage lives in `tests/tsa-bonno-brand-migration.test.mjs` when dist artifacts exist. A disposable copy of existing `boroko-bookings` user-data was opened by the branded LodgingOS binary with profile/updater hashes preserved and the real AppData directory left unchanged; full NSIS in-place upgrade remains unproved. A running HotelOS window remained `Botswapelo Hotel · Tsa Bonno HotelOS` through LodgingOS and POS builds. All installers are unsigned (`NotSigned`), so publication remains blocked. Existing release feed URLs remain compatibility channels. | Code-sign with a real Authenticode certificate, prove clean installs and full NSIS in-place LodgingOS upgrade/data retention on a disposable machine/copy, then publish serially without changing update feeds. |
| Legacy POS installer | proved locally | renamed installer not published | local IA-32/metadata proof; POSReady/hardware smoke not-proven | Legacy POS regression 216/216 and Electron 22 build pass. `Tsa-Bonno-POS-Legacy-1.1.0-ia32.exe` and the packaged app are IA-32 (`0x014C`) with exact Tsa Bonno metadata; `latest.yml` references the IA-32 artifact. The installer is unsigned (`NotSigned`). | Code-sign, run POSReady/hardware/database-probe smoke, then publish to the preserved Legacy POS feed. |
| Manager PWA | proved | proved | proved for public shell/brand HTTP smoke | Vercel production deployment `dpl_DpgoSFHcoJFFGyZ35o2J4vhinXPm`; both new `tsa-bonno-hospitalityos-manager.vercel.app` and legacy `boroko-bookings.vercel.app` return HTTP 200 with the Tsa Bonno title. The live official HospitalityOS logo hash matches the local production asset. Local build passes. | Smoke authenticated workflows for all three product-family memberships. |
| Public booking site | proved | proved | proved for production asset/copy and HTTP/SPA fallback; business flow not-proven | Vitest 33/33 and production build pass. Netlify production deploy `6a5553d714f85b1656c0eaa9` is live at the compatibility URL `borokoonlinebookings.netlify.app`; the deployed LodgingOS logo hash matches locally and the bundle includes the visible LodgingOS footer. | Smoke real public booking, campsite, confirmation-email, and guest-portal workflows against a test property. |
| Marketing site | proved | proved | proved for public brand/pages/PDF/function error path | Netlify production deploy `6a5548877999632bcfd3426c`; the live official HospitalityOS logo hash matches locally. Homepage, packages, LodgingOS, HotelOS, Restaurant & Bar POS, admin export name, brochure PDF, and quote-function missing-token path were checked. | Add a canonical Tsa Bonno marketing domain/email aliases when supplied; continue lead-email and conversion monitoring. |
| Payment provider integration | local-only | not-proven | not-proven | Local security gates prove renderer/desktop cannot fake webhook settlement and server RPC is service-role-only. | Implement/configure real hosted checkout plus server-side webhook endpoint, provider credentials, reconciliation, duplicate webhook smoke, and live/provider test-mode evidence. |
| Channel/OTA provider integration | local-only | not-proven | not-proven | Local gates prove Channel Manager fails closed/manual-review without a live provider. | Connect real OTA provider adapter, prove provider acknowledgements, import/export handling, conflict/manual-review flow, and retry/dead-letter behavior. |
| Custom website deployment automation | local-only | not-proven | not-proven | Enterprise readiness screens expose unresolved launch gates. | Implement deployment automation, publish a generated/custom site, and smoke public routes/forms/booking handoff. |

Do not describe a surface as released, published, deployed, or operational unless the relevant row is `proved` for that claim.
