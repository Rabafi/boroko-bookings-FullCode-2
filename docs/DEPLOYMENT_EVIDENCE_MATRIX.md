# Deployment Evidence Matrix

## 2026-08-21 — Offline contract, secure-store, health-fault, and setup-read truth tranche

Linked migration `20260821040000_offline_rpc_contracts_and_desktop_grants.sql` was successfully pushed; `supabase migration list --linked` reports local/remote parity through `20260821040000`. Live anonymous probes confirmed both new offline wrappers are callable but fail closed with `Access denied for this lodge.`, while direct `clock_out_staff` returns permission denied. The tranche adds stable idempotent POS-menu/staff-clock-out wrappers with legacy queue replay conversion, fail-closed encrypted secure storage with propagated warning, bounded/redacted deterministic health-fault retention/dedupe on the actual sync-store path, and an authoritative setup-read envelope that labels offline/unavailable evidence and blocks completion until server evidence is complete.

Local validation: combined focused coverage 11/11, `npm run test:bar` 171/171, node checks pass, and `npm run build:hospitality-pos` exits 0 with the existing Vite mesh import warning. Linked SQL lint returned `results=[]`; the pre-deploy dry run identified only `20260821040000`. A later post-deploy dry run hit transient `LegacyDbConfigConnectTempRoleError`, and linked advisors stalled during login-role initialization and were interrupted, so neither is claimed as a post-deploy pass. Desktop source/build evidence remains local-only: no installer/public/PWA publication or operator smoke has been completed.

Count correction (2026-08-14): the Bar suite is 98/98 after adding owner-digest, staff-performance, recipe-variance, finance-overview, and cash-up history-unavailable regressions; earlier 84/84, 85/85, 86/86, 89/89, and 94/94 references below are superseded.

Linked deployment correction (2026-08-14): Docker/Podman are unavailable locally, so the ordered chain was applied through `20260814080000_security_definer_search_path_hardening.sql` with `supabase db push --linked --yes`. `supabase migration list --linked` shows 371 local/remote IDs with no pending migrations, `supabase db lint --linked --level error --fail-on error` passes, and `supabase db advisors --linked --level error --fail-on error` is clean. Read-only smoke confirms the authoritative POS tab link, cutover approval evidence, partial tax-return uniqueness, absence of obsolete `pos_payments` references, security-invoker compatibility views, no direct anon/authenticated grants on the ten internal tables, only the canonical active-table uniqueness index remains, and 59 SECURITY DEFINER functions have a fixed trusted search path. Local disposable behavior/concurrency tests remain unavailable without a local PostgreSQL runtime. This is database deployment evidence, not a release sign-off.

Final cross-surface verification (2026-08-14; superseded security wording): generic restaurant Finance Overview/Daily Close/Cash Drawer, desktop POS History, Hpos My Sales, Legacy receipt detail, open-tab split previews, customer display, queued return rows, incomplete cash-up previews, and cash-up history reads fail closed on uncertified totals/tenders or unavailable sources. Owner Digest, staff performance, recipe variance/preparation-loss, finance overview, settlements, deposits, tips, and cash-up reads now preserve source completeness and do not turn failed reads into zero totals. Online receipt DTOs preserve missing server amounts/tenders as unavailable instead of zero/local fallback truth. Detailed expenses and stock-cost reports/exports now require explicit server-complete source metadata and never reconstruct missing totals from quantity × unit cost; shared booking revenue, occupancy, P&L, and room-profitability views use the same finality contract. The finality and authorization migrations are applied to the linked project; local evidence is 98/98 Bar tests, 189/189 focused financial-truth tests, 217/217 Legacy POS regression tests, plus green offline/queue, enterprise/release, production guardrails, web-surface, `audit:prod`, Manager lint/build, and Hospitality POS build gates. The current correction below supersedes the historical advisor count: error-level advisors are now clean, but no disposable PostgreSQL or authenticated production smoke has run and policy/sign-off gates remain open.

Current correction for this section (2026-08-14): local evidence is 98/98 Bar tests, 189/189 focused financial-truth tests, and 217/217 Legacy POS regression tests; offline POS/queue critical gates, enterprise/release guardrails, full production guardrails, and web-surface contracts/builds also pass; the production dependency audit reports 0 vulnerabilities. The linked project is applied through `20260814080000`; all 371 local/remote migration IDs match, linked SQL lint is clean at error level, and error-level security advisors are clean. The no-ship lockdown remains active for Accounting report, export, payroll, AP, tax, bank, cutover, and the new Bar/POS/report finality functions. Disposable PostgreSQL behavior/concurrency tests, authenticated outlet-isolation smoke, and policy sign-off remain open.

| Financial-truth cutover artifact | Local state | Linked/production state | Required evidence |
| --- | --- | --- | --- |
| `20260807390000_historical_cutover_audit_and_approval.sql` | present; contract-tested; not applied | not-proven | Apply through disposable DB, verify SQL behavior/concurrency/isolation, deploy forward migration, verify grants/RLS/catalog/lint/advisors, then record a reviewer-approved packet per lodge. |
| `20260807400000_pos_return_authoritative_reversal.sql` | present; contract-tested; not applied | not-proven | Apply through disposable DB, prove return/tender/recipe/direct-stock/COGS rollback and retry behavior, then deploy and verify the v3 compatibility grant and authenticated smoke. |
| `20260807410000_ap_supplier_controls_and_credit_notes.sql` | present; contract-tested; not applied | not-proven | Apply through disposable DB, prove multi-line bill/evidence, maker-checker credit notes, supplier/AP-control reconciliation, credit-aware payment locking, RLS/grants, and authenticated desktop smoke. |
| `20260807420000_accounting_no_ship_grant_lockdown.sql` | present; contract-tested; not applied | not-proven | Verify authenticated denial of Accounting report forgery/export/cutover calls, generic POS/lodge report independence, service-role-only Accounting paths, and deep-link release gating. |
| `20260807430000_statement_history_and_opening_balance_truth.sql` | present; contract-tested; not applied | not-proven | Prove inactive historical accounts, scalar-opening disposition, deactivation guards, arbitrary-period balances, fail-closed statement status, and activation blocking. |
| `20260807440000_pos_operational_tender_subledger.sql` | present; contract-tested; not applied | not-proven | Prove Growth account/voucher operation without Accounting, per-tender uniqueness, feature gates, row locks, rollback, and Accounting-active balanced GL posting. |
| `20260807450000_pos_return_cumulative_reversal.sql` | present; contract-tested; not applied | not-proven | Prove cumulative tip/tender rounding, voucher/account restoration, original cost reversal, concurrent return limits, exact retry, and conflicting retry denial. |
| `20260807460000_financial_source_coverage_registry.sql` | present; contract-tested; not applied | not-proven | Prove all enabled source classes are present, unsupported/pending/failed sources block completeness, and inactive Accounting returns `not_active`/`complete=false`. |
| `20260807470000_bank_reconciliation_semantics.sql` | present; contract-tested; not applied | not-proven | Prove debit/credit signs, running balances, duplicate/split allocations, maker-checker, over-allocation locks, cross-lodge isolation, and immutable packets. |
| `20260807480000_accounting_export_finality.sql` | present; contract-tested; not applied | not-proven | Prove page-specific export filters and watermarks, approved-period payroll selection, stale/unreconciled blocking, detailed evidence, and finality fields. |
| `20260807490000_payroll_pii_and_statutory_gate.sql` | present; contract-tested; not applied | not-proven | Prove governed source documents, SHA-256 validation, independent statutory approval, missing-worker blocking, masked reads, and audited raw payment export capability. |
| `20260807500000_accounting_export_companion_evidence.sql` | present; contract-tested; not applied | not-proven | Prove successful file writes alone cannot certify artifacts, PDF companion hash/path linkage, artifact failure retention, RLS, and service-role-only recording. |
| `20260807510000_pos_report_control_parity.sql` | present; contract-tested; not applied | not-proven | Prove server POS controls match screen/export classification, split tender identity, business-date filtering, item cost evidence, and uncapped exports. |
| `20260807520000_pos_tender_and_bank_lock_hardening.sql` | present; contract-tested; not applied | not-proven | Prove multiple account/voucher tenders, cumulative return corrections, bank allocation locks, journal capacity, completed-packet immutability, and maker-checker behavior. |
| `20260807530000_financial_source_expected_rows_and_accounting_lockdown.sql` | present; contract-tested; not applied | not-proven | Prove authoritative source populations cannot be silently omitted, subledger-to-GL controls reconcile, and every Accounting RPC remains service-role-only while no-ship is active. |
| `20260807540000_statement_finality_and_cash_flow_classification.sql` | present; contract-tested; not applied | not-proven | Prove historical inactive accounts, source coverage, cash-flow classification, balance equality, close state, and finality fields across arbitrary periods. |
| `20260807550000_bank_match_allocation_workspace.sql` | present; contract-tested; not applied | not-proven | Prove candidate lodge isolation, allocation-aware workspace reads, split/combined match capacity, maker-checker review, completed-packet immutability, and authenticated bank smoke. |
| `scripts/financial-truth-cutover-audit.mjs` | present; read-only default; contract-tested | not-run against target lodge | Run dry-run, retain packet hash/source manifest/control totals, then separately run prepare/approve/activate only after all no-ship gates pass. |

## 2026-08-07 financial-truth control-plane boundary

The local tip now includes `20260807550000_bank_match_allocation_workspace.sql`; it is local-only and has not been deployed or enabled. The no-ship grant lockdown remains active. The account/voucher guard, return correction, AP correction, source-coverage registry, bank allocation workspace, export finality, payroll, source-population, and statement-finality controls must be behavior-tested together before any authenticated grant is restored.

Correction to the historical metrics below: current local evidence is 179/179 focused financial-truth tests, 217/217 Legacy POS regression tests, and 31 earlier migrations through `20260807380000` compiled in a transaction and rolled back. Migrations `20260807390000` through `20260807550000` have static/contract evidence only. The desktop full data exporter now emits `data-export-manifest-v1`, blocks incomplete financial exports, and visibly labels allowed partial operational exports. The disposable restaurant harness is present and fail-closed, but cannot run here because Docker Desktop/Podman is unavailable; an elevated retry confirmed the blocker as `docker: command not found`.

The Bar/Accounting financial-truth implementation is local-only in this worktree. The forward migrations `20260807390000` through `20260807500000` have not been applied to the linked database, and no accounting lodge has been enabled from this worktree. A linked `db lint` query reached Supabase on 2026-08-07 but reported 20 existing SQL errors; it is not a clean deployment gate. The required disposable PostgreSQL behavior/concurrency suite, authenticated source-coverage/cutover smoke, cash-up variance/settlement rehearsal, payroll settlement/bank rehearsal, tax amendment/statutory policy review, and production migration evidence are hard gates.

Local evidence completed on 2026-08-07: focused financial-truth/accounting contract coverage passes 172 tests including cash-up, tax lifecycle, adjustment-read, all-eight Accounting-page export contracts, POS classifier/return truth, AP supplier controls, companion artifact guards, and complete JSON/XLSX/CSV/PDF file-export guards; Manager PWA lint has 0 errors and 45 warnings; Manager PWA build passes; Hospitality POS production build passes; JavaScript syntax checks pass for the changed desktop bridges/domains; and a temporary transaction validator compiled 31 earlier migrations through `20260807380000` against the linked schema before rolling them back. The nine newer forward migrations are not included in that compile result. The full restaurant behavioral suite still cannot reach its disposable database gate because PostgreSQL at `127.0.0.1:54322` is unavailable. Linked lint reached the project and captured 20 errors from the currently deployed schema; the local migrations are not deployed, so this remains a no-ship blocker, not deployment proof.

As of: 2026-08-14

This file records release and deployment evidence for the current Tsa Bonno HospitalityOS worktree. It is not a substitute for the release runbook; it is the place where the runbook's deployment matrix is made explicit.

Status values:

- `proved`: current evidence proves the row for this worktree/date.
- `local-only`: local code/build/test evidence exists, but publication or production smoke is not proven.
- `not-proven`: no current evidence in this worktree proves the row.

| Surface | Local verification | Published or deployed | Smoke-tested | Current evidence | Remaining proof required |
| --- | --- | --- | --- | --- | --- |
| Supabase migrations and functions | proved | proved for linked project | proved for dashboard identity, brand-label RPC, and function deployment | Linked migration history matches through `20260820170000`; the post-deployment dry run reports the remote database up to date, error-level SQL lint returns zero findings, and error-level advisors return no issues. Migration `20260713013000` appears in both local and remote history. Live anonymous RPC calls return Tsa Bonno LodgingOS, HotelOS, and Restaurant & Bar POS. `send-booking-confirmation` is redeployed with Tsa Bonno copy. Authenticated Management API and CLI verification report project display name `Tsa Bonno HospitalityOS` while ref `oicgpknsmtvcsjacymum` remains unchanged. | Run isolated customer-data/provider smoke where required. |
| Desktop product family | proved | prior releases only; renamed release not published | local packaged asar isolation + metadata proved; disposable user-data load proved; operator/clean-machine/NSIS in-place upgrade smoke not-proven | LodgingOS, HotelOS, and Restaurant & Bar POS compile under isolated `out/<product>` trees and package only their own output. Independent `@electron/asar` inspection of each `dist/*/win-unpacked/resources/app.asar` found zero foreign `out/<other-product>/` paths, correct packaged mains, correct `product.json`, correct preserved update repos in `app-update.yml`, official product logo resources, bridge package names (`boroko-bookings` etc.), and `deleteAppDataOnUninstall: false`. Fresh v1.5.5 installers have branded icons and exact Tsa Bonno `ProductName`/`FileDescription` metadata. Automated packaging-isolation coverage lives in `tests/tsa-bonno-brand-migration.test.mjs` when dist artifacts exist. A disposable copy of existing `boroko-bookings` user-data was opened by the branded LodgingOS binary with profile/updater hashes preserved and the real AppData directory left unchanged; full NSIS in-place upgrade remains unproved. A running HotelOS window remained `Botswapelo Hotel · Tsa Bonno HotelOS` through LodgingOS and POS builds. All installers are unsigned (`NotSigned`), so publication remains blocked. Existing release feed URLs remain compatibility channels. | Code-sign with a real Authenticode certificate, prove clean installs and full NSIS in-place LodgingOS upgrade/data retention on a disposable machine/copy, then publish serially without changing update feeds. |
| Legacy POS installer | local-only | renamed installer not published | regression 217/217; `npm run legacy-pos:build` passes after installing the already-declared locked dependency set with an isolated package store | Existing `Tsa-Bonno-POS-Legacy-1.1.0-ia32.exe` and packaged app are IA-32 (`0x014C`) with exact Tsa Bonno metadata; `latest.yml` references the IA-32 artifact. The installer is unsigned (`NotSigned`). | Run POSReady/hardware/database-probe smoke, code-sign, then publish to the preserved Legacy POS feed. |
| Manager PWA | proved | proved | proved for public shell/brand HTTP smoke | Vercel production deployment `dpl_DpgoSFHcoJFFGyZ35o2J4vhinXPm`; both new `tsa-bonno-hospitalityos-manager.vercel.app` and legacy `boroko-bookings.vercel.app` return HTTP 200 with the Tsa Bonno title. The live official HospitalityOS logo hash matches the local production asset. Local build passes. | Smoke authenticated workflows for all three product-family memberships. |
| Public booking site | proved | proved | proved for production asset/copy and HTTP/SPA fallback; business flow not-proven | Vitest 33/33 and production build pass. Netlify production deploy `6a5553d714f85b1656c0eaa9` is live at the compatibility URL `borokoonlinebookings.netlify.app`; the deployed LodgingOS logo hash matches locally and the bundle includes the visible LodgingOS footer. | Smoke real public booking, campsite, confirmation-email, and guest-portal workflows against a test property. |
| Marketing site | proved | proved | proved for public brand/pages/PDF/function error path | Netlify production deploy `6a5548877999632bcfd3426c`; the live official HospitalityOS logo hash matches locally. Homepage, packages, LodgingOS, HotelOS, Restaurant & Bar POS, admin export name, brochure PDF, and quote-function missing-token path were checked. | Add a canonical Tsa Bonno marketing domain/email aliases when supplied; continue lead-email and conversion monitoring. |
| Payment provider integration | local-only | not-proven | not-proven | Local security gates prove renderer/desktop cannot fake webhook settlement and server RPC is service-role-only. | Implement/configure real hosted checkout plus server-side webhook endpoint, provider credentials, reconciliation, duplicate webhook smoke, and live/provider test-mode evidence. |
| Channel/OTA provider integration | local-only | not-proven | not-proven | Local gates prove Channel Manager fails closed/manual-review without a live provider. | Connect real OTA provider adapter, prove provider acknowledgements, import/export handling, conflict/manual-review flow, and retry/dead-letter behavior. |
| Custom website deployment automation | local-only | not-proven | not-proven | Enterprise readiness screens expose unresolved launch gates. | Implement deployment automation, publish a generated/custom site, and smoke public routes/forms/booking handoff. |

Do not describe a surface as released, published, deployed, or operational unless the relevant row is `proved` for that claim.
