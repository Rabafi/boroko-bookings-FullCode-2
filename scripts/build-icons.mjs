// Backward-compatible entry point. The canonical generator now owns every
// ecosystem and product logo/icon so older release commands cannot overwrite
// the official owner-supplied artwork with a generic mark.
await import('./build-brand-assets.mjs')
